---
// Temporary wrapper component, until we can get everything migrated over to components
import { Embed as ComponentEmbed } from "selo-components";
interface Props {
  id: string;
}
const { id } = Astro.props;
---

<ComponentEmbed id={id} />
