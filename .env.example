# Base URL for the `kalama` audio assets (words, names, sfx).
# Leave unset for the website (defaults to the remote GitHub mirror).
# For the Android/app build, point this at the bundled local copy so
# audio works offline, e.g.:
# PUBLIC_KALAMA_BASE=/kalama
